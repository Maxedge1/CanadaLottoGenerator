## CLI

`python -m canada_lotto_generator` runs the CLI. Examples:

Generate 3 draws with bonus as JSON:

```bash
python -m canada_lotto_generator -n 3 --bonus --format json
```

Use the `--preset` and `--seed` options for deterministic draws.
